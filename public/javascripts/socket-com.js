(function(){
    YUI().add('socket-com', function(Y) {
        var port = 8765;
        var socket = {
            connect: function() {
                
            }
        };
    }, '0.0.1', {requires:[]});  
})();
